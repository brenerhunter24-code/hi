<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Platform Shrink Survival</title>
<style>
  body {
    margin: 0;
    background: #111;
    overflow: hidden;
    font-family: Arial, sans-serif;
    color: white;
  }
  #ui {
    position: absolute;
    top: 10px;
    left: 10px;
    font-size: 20px;
  }
</style>
</head>
<body>

<div id="ui">
  Time Survived: <span id="time">0</span>s
</div>

<canvas id="game"></canvas>

<script>
const canvas = document.getElementById("game");
const ctx = canvas.getContext("2d");
canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

const keys = {};
document.addEventListener("keydown", e => keys[e.key] = true);
document.addEventListener("keyup", e => keys[e.key] = false);

let timeSurvived = 0;
let lastShrink = 0;

const player = {
  x: canvas.width / 2,
  y: canvas.height / 2,
  w: 30,
  h: 30,
  vx: 0,
  vy: 0,
  speed: 6
};

let platform = {
  x: canvas.width / 2 - 200,
  y: canvas.height / 2 - 20,
  w: 400,
  h: 40
};

let hazards = [];

function spawnHaz
